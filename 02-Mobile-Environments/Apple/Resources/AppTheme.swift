import SwiftUI

struct AppTheme {
    static let primaryBlue = Color(red: 0.0, green: 0.4, blue: 0.8)
    static let backgroundGray = Color(white: 0.95)
    static let successGreen = Color.green
    static let errorRed = Color.red
}
